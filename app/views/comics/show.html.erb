<h2><%= @comic.name %></h2>
<div>
  The comic <%= @comic.name %>, published by <%= @comic.publisher %>.
  Mira contributed as a <%= @comic.role %>.
</div>
<br/>
<div>
  <% if @comic.products.present? %>
  BUY:
  <% end %>
  <% @comic.products.each do |product| %>
    <%= link_to image_tag(product.vendor.file, alt: product.vendor.name, height:'40'),
        product.url %>
  <% end %>
</div>
